<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- FILE-ONLY logging: stdout is reserved for JSON-RPC in STDIO mode -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/mcp-server.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/mcp-server.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.omnibridge.mcp" level="INFO"/>
    <logger name="io.modelcontextprotocol" level="INFO"/>
    <logger name="org.rocksdb" level="WARN"/>
    <logger name="net.openhft" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="FILE"/>
    </root>
</configuration>
